image: cc_image:latest

before_script:
  - conda update -y setuptools
  - pip install tox
  - pip install tox-conda
  - pip install --upgrade pip


test:
  script:
    - cd package

pages:
  script:
  - pip install sphinx
  - sphinx-apidoc -o source package/chemicalchecker -f -e
  - make html
  - mv _build/html/ public/
  artifacts:
    paths:
    - public
  only:
  - master
